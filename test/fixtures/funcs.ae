// Raise to power
def pow(base exp) :: (flt flt) -> flt
// Compute square root
def sqrt(x) :: flt -> flt
// Compute cubic root
def cbrt(x) :: flt -> flt
// Compute hypotenuse
def hypot(x y) :: (flt flt) -> flt (
)
def hypot(x y) (  // inferred type
    sqrt(x² + y²)
)

// one-liners
def sqr(x) x*x
def sqr(x) (x**2)
def cub(x) x³+1
